<div class="p-8">
    <h1 class="text-4xl md:text-6xl text-{{ $color }}">{{ mb_strtoupper($title) }}</h1>
    @if($subtitle)
    <h2 class="text-3xl md:text-5xl text-{{ $color }} mt-3">{{ $subtitle }}</h2>
    @endif
</div>
